interface Config {
  rpcUrl: string;
  publicKey: string;
  privateKey: string;
  contractAddress: string;
  pgUser: string;
  pgPassword: string;
  pgHost: string;
  pgPort: number;
  pgDatabase: string;
}
